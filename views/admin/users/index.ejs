<%-include ('../../partials/header')%>

<body>
    <%-include ('../../partials/navbar')%>
    
    <div class="container mt-5">
        <h2>Usuários</h2>

        <a href="/admin/users/create" class="btn btn-success mt-3">Criar Novo Usuario</a>

        <table class="table mt-3">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Email</th>
                </tr>
            </thead>

            <tbody>
                <%users.forEach((user)=>{%>
                    <tr>
                        <td><%=user.id%></td>
                        <td><%=user.email%></td>
                    </tr>
                <%})%>
            </tbody>
        </table>
    </div>


</body>


<%-include ('../../partials/footer')%>